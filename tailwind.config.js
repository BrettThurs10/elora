module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {
            width: ['hover', 'focus'],
            transitionProperty: ['hover', 'focus'],
        },
    },
    plugins: [],
}
